import styled from 'styled-components';

const Wrapper = styled.div`
  figure {
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background-color: white;
  }
`;

const StatsList = styled.ul`

`
const UserName = styled.p`
font-size: 25px;
font-weight: 700;
`

export {Wrapper, StatsList, UserName}